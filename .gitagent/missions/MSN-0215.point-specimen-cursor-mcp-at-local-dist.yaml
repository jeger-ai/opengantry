# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Point specimen Cursor MCP at local dist/cli so gxt_propose_contract and gxt_draft_legislation.contract appear in the live catalog. Assert v3 schema in dogfood by passing a contract into handleDraftLegislation and requiring contract_sha256 on the written YAML. Document MCP restart. Do not re-implement mcp-*.ts handlers.
msn_id: MSN-0215
skill_key: substrate
gate_command: ./scripts/dev-validate-core.sh
trace_rows:
  - dod_id: "1"
    trace_quote: "DoD 1 MSN-0215: specimen .cursor/mcp.json launches node dist/cli/index.js mcp serve; gxt_draft_legislation schema includes contract; dogfood passes contract into handleDraftLegislation and requires contract_sha256; OK: MCP dogfood flow passed; tests 752 pass 752 fail 0; ok: substrate version: 3.4.0 (matches bundled gantry); check-changed-code OK; MSN commit subjects OK (path-scoped: substrate + MANIFEST tmvc_roots); dev-validate-core OK — stack: check, manifest, tests, doctor, changed-code, MSN"
    anchor: "964"
    status: PASS
interrogation:
  - finding_id: 1362fca0b3bc
    kind: risk_escalation
    question: "Triage escalated: Intent contains risk_keyword: schema. Which
      skill_key and TMVC scope authorize this work?"
    hypothesis: Planner assigns explicit skill_key and narrows TMVC in the mission
      after operator confirmation.
    operator_answer: "skill_key=substrate. Authorize only: .cursor/mcp.json,
      docs/DEVELOPMENT.md, docs/INTEGRATIONS.md,
      scripts/validate-mcp-dogfood.mjs,
      src/cli/tests/mcp-tools-register.test.ts, EXECUTOR_LOG.md, and the
      MSN-0215 mission YAML. Do not re-implement mcp-*.ts. Do not edit
      templates/.cursor/mcp.json or templates/integrations/recipes/cursor.md.
      Gate ./scripts/dev-validate-core.sh."
    adr_refs:
      - ADR-0045
  - finding_id: 2121e0ae69d9
    kind: adr_conflict
    question: Intent may relate to ADR-0037. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0037 before
      legislation proceeds.
    operator_answer: "Reject applicability: MSN-0215 does not change Plane
      deployment or CI ingestion. It wires specimen Cursor MCP to local dist/cli
      and mandates a contract on the MCP dogfood draft."
    adr_refs:
      - ADR-0037
  - finding_id: 5a4bf18c0690
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: Authorize src/cli/tests/mcp-tools-register.test.ts only to
      assert gxt_draft_legislation schema includes contract. No mcp-*.ts handler
      edits. Remaining paths are Cursor mcp.json, DEVELOPMENT.md,
      INTEGRATIONS.md, validate-mcp-dogfood.mjs, EXECUTOR_LOG.md, and the
      mission YAML.
  - finding_id: a7bb97b7d55a
    kind: adr_conflict
    question: Intent may relate to ADR-0022. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0022 before
      legislation proceeds.
    operator_answer: "Accept ADR-0022: MCP write-containment stays in force. This
      mission does not edit mcp-write-guard.ts or widen gxt_* writes. It points
      specimen Cursor at local dist/cli and dogfoods a sealed contract on
      draft/execute."
    adr_refs:
      - ADR-0022
  - finding_id: e98547caadb1
    kind: adr_conflict
    question: Intent may relate to ADR-0045. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0045 before
      legislation proceeds.
    operator_answer: "Accept ADR-0045: live Cursor must serve the sealed-cage MCP
      surface. Dogfood must pass a contract into handleDraftLegislation and
      require contract_sha256 on the written YAML. No-cage draft is a v2 false
      positive."
    adr_refs:
      - ADR-0045
interrogation_sha256: 5db511335fed9c66c2221c65f0267eef6163120035a5fb18919b922a286a79e2
declared_paths:
  - .cursor/mcp.json
  - .gitagent/missions/MSN-0215.point-specimen-cursor-mcp-at-local-dist.yaml
  - EXECUTOR_LOG.md
  - docs/DEVELOPMENT.md
  - docs/INTEGRATIONS.md
  - scripts/validate-mcp-dogfood.mjs
  - src/cli/tests/mcp-tools-register.test.ts
contract:
  tmvc_roots:
    - .cursor/mcp.json
    - EXECUTOR_LOG.md
    - docs/DEVELOPMENT.md
    - docs/INTEGRATIONS.md
    - scripts/validate-mcp-dogfood.mjs
    - src/cli/tests/mcp-tools-register.test.ts
  forbidden_zones:
    - src/auth/
    - src/core/
    - src/db/schema/
contract_sha256: 1039463d53bf9c189177bd4e888a313d600acd0b3bf2b407be6b11862d2557f3
