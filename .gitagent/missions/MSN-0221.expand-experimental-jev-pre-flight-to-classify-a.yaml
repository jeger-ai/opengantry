# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Expand experimental Jev pre-flight to classify any active MANIFEST skill. Keep Jev advisory fail-open. Tests in src/cli/tests/contract-preflight.test.ts use os.tmpdir() fixtures for substrate api frontend. Do not change production MANIFEST.json. Do not modify src/cli/lib/triage-logic.ts.
msn_id: MSN-0221
skill_key: gantry
gate_command: npm test
trace_rows:
  - dod_id: "1"
    trace_quote: REPLACE_WITH_VERBATIM_QUOTE_FROM_EXECUTOR_LOG_AFTER_EXECUTION
    anchor: "1"
    status: PENDING
interrogation:
  - finding_id: 17b1813a0fa9
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: Authorized. Work stays in src/cli/lib/contract/ and
      src/cli/tests/contract-preflight.test.ts. Multi-skill fixture trees live
      only under os.tmpdir() and are removed in afterAll; never create src/api/
      or src/ui/ at the repository root. Tests live under src/cli/tests/; no
      TARGET_ARCHITECTURE layer change.
  - finding_id: 9643d0bc591b
    kind: adr_conflict
    question: Intent may relate to ADR-0025. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0025 before
      legislation proceeds.
    operator_answer: Reject applicability. The advisory match is Jev contract
      preflight (ADR-0046), not ARCHITECTURE_RUBRIC LLM judge. This mission does
      not add a rubric judge or GXT-ARCH-OVERRIDE.
    adr_refs:
      - ADR-0025
      - ADR-0046
  - finding_id: b9b235d7d426
    kind: adr_conflict
    question: Intent may relate to ADR-0046. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0046 before
      legislation proceeds.
    operator_answer: "Accept ADR-0046: Jev remains experimental advisory preflight.
      Timeouts, HTTP errors, and schema validation failures fail open to the
      offline heuristic; MCP must not throw. Preflight still does not write
      contract or become mission law. Expand the test matrix to all manifest
      skills; do not add fictional frontend or api keys to production
      MANIFEST.json."
    adr_refs:
      - ADR-0046
  - finding_id: c381e0aea4c3
    kind: risk_escalation
    question: "Triage escalated: Intent references path under path_risks: src/cli/.
      Which skill_key and TMVC scope authorize this work?"
    hypothesis: Planner assigns explicit skill_key and narrows TMVC in the mission
      after operator confirmation.
    operator_answer: Legislate MSN-0221 under skill gantry. Lock TMVC to
      src/cli/lib/contract/ and src/cli/tests/contract-preflight.test.ts. Do not
      include src/cli/lib/triage-logic.ts; the offline heuristic already maps
      non-gantry intents from the loaded fixture manifest. Gate is npm test.
    adr_refs:
      - ADR-0046
      - ADR-0045
  - finding_id: e2a17b81b981
    kind: adr_conflict
    question: Intent may relate to ADR-0035. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0035 before
      legislation proceeds.
    operator_answer: Reject applicability. The advisory match is Jev contract
      preflight (ADR-0046), not PERFORMANCE_RUBRIC LLM judge. This mission does
      not add a performance judge.
    adr_refs:
      - ADR-0035
      - ADR-0046
  - finding_id: e98547caadb1
    kind: adr_conflict
    question: Intent may relate to ADR-0045. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0045 before
      legislation proceeds.
    operator_answer: "Accept ADR-0045: proposeContract / gxt_propose_contract stay
      deterministic and LLM-free. This mission seals a Planner contract for the
      test-matrix TMVC; Jev preflight still does not write contract or
      contract_sha256."
    adr_refs:
      - ADR-0045
interrogation_sha256: 7612652088003d9d50ce779bf7434980aac7ae511fff5518a6da80b0c7e79032
declared_paths:
  - src/cli/lib/contract/
  - src/cli/tests/contract-preflight.test.ts
contract:
  tmvc_roots:
    - src/cli/lib/contract/
    - src/cli/tests/contract-preflight.test.ts
  forbidden_zones:
    - .gitagent
    - .gitagent/foreman/
    - .gitagent/planner/RULES.md
    - src/auth/
    - src/core/
    - src/db/schema/
  allowed_imports:
    - ${spec}
    - yaml
  banned_imports:
    - "@modelcontextprotocol/sdk/client/index.js"
    - "@modelcontextprotocol/sdk/client/stdio.js"
    - commander
contract_sha256: 467749893f6d980222ebfb1ef8c3e9760138c3df1c6f4618670045c7da61af4b
