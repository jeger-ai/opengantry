# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Implement ADR-0040 in gantry CLI: envelope_schema_version 3 with exact and semantic fingerprints, byte-safe evidence truncation, ENOENT-safe blame projection from import-layer and banned-import parsers, compact NEXT_REMEDIATION.json with findings and gate_log_path, 4-slot semantic digest ring circuit breaker aborting with GXT_FINDINGS_RECURRED, SARIF region mapping, fix offending_file doc drift.
msn_id: MSN-0179
skill_key: gantry
gate_command: npm test
trace_rows:
  - dod_id: "1"
    trace_quote: "DoD 1 MSN-0179: envelope_schema_version 3 with exact/semantic fingerprints, byte-safe evidence truncation, import-layer and banned-import gate projection, compact NEXT_REMEDIATION v2 with findings and gate_log_path, 4-slot semantic digest ring aborting GXT_FINDINGS_RECURRED, SARIF rule_id/span mapping, offending_file doc alignment; npm test 551 pass"
    anchor: "799"
    status: PASS
interrogation:
  - finding_id: 1362fca0b3bc
    kind: risk_escalation
    question: "Triage escalated: Intent contains risk_keyword: schema. Which
      skill_key and TMVC scope authorize this work?"
    hypothesis: Planner assigns explicit skill_key and narrows TMVC in the mission
      after operator confirmation.
    operator_answer: "Split: substrate mission for ADR-0040 only, then gantry
      mission for the CLI implementation. Execute MSN-0179 to implement the CLI
      contract. skill_key gantry; TMVC src/cli/."
    adr_refs:
      - ADR-0040
      - ADR-0032
  - finding_id: 2121e0ae69d9
    kind: adr_conflict
    question: Intent may relate to ADR-0037. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0037 before
      legislation proceeds.
    operator_answer: Reject ADR-0037 applicability. Plane/CI ingest is out of scope;
      this mission implements the local gantry verify findings contract
      (ADR-0040).
    adr_refs:
      - ADR-0037
  - finding_id: 9ecdd9a8e878
    kind: adr_conflict
    question: Intent may relate to ADR-0027. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0027 before
      legislation proceeds.
    operator_answer: Accept ADR-0027. SARIF/JUnit remain; v3 maps rule_id and spans
      into SARIF region. The revised plan is airtight.
    adr_refs:
      - ADR-0027
  - finding_id: a7bb97b7d55a
    kind: adr_conflict
    question: Intent may relate to ADR-0022. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0022 before
      legislation proceeds.
    operator_answer: Reject ADR-0022 applicability — spurious match on
      GXT_FINDINGS_RECURRED vs gxt_; this mission only writes an ADR under
      out-of-scope, not MCP tool write paths
    adr_refs:
      - ADR-0022
  - finding_id: aaba7dc586f8
    kind: adr_conflict
    question: Intent may relate to ADR-0040. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0040 before
      legislation proceeds.
    operator_answer: Accept ADR-0040. Proceed with legislating MSN-0178 to ratify
      ADR-0040. Once the substrate gate passes, execute MSN-0179 to implement
      the CLI contract.
    adr_refs:
      - ADR-0040
  - finding_id: c2f468809c0c
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: Confirmed — gantry skill src/cli/ TMVC; src/cli/tests/ is
      co-located coverage for the CLI contract (fingerprints, ring breaker,
      truncation, ENOENT, context-feed). Execute MSN-0179 to implement the CLI
      contract.
interrogation_sha256: 64834c4d2cd37976a6ae5b6370739289ca0b5e32161cd36c14ccafc9bda19bc5
declared_paths:
  - README.md
  - docs/AGENT-GRAPH.md
  - docs/FEATURES.md
  - docs/INTEGRATIONS.md
  - docs/USE-CASES.md
  - src/cli/lib/banned-import-violation.ts
  - src/cli/lib/context-feed-remediation.ts
  - src/cli/lib/context-feed-store.ts
  - src/cli/lib/gxt-error-codes.ts
  - src/cli/lib/surgeon.ts
  - src/cli/lib/verify-export.ts
  - src/cli/lib/verify-failure.ts
  - src/cli/lib/verify-finding.ts
  - src/cli/lib/verify-payload.ts
  - src/cli/lib/verify-run.ts
  - src/cli/tests/
