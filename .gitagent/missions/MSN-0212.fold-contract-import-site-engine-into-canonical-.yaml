# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Fold contract import-site engine into canonical import-scanner under src/cli/. Gate npm test. MCP propose skill_key required. Do not edit .gitagent/.
msn_id: MSN-0212
skill_key: gantry
gate_command: npm test
trace_rows:
  - dod_id: "1"
    trace_quote: "DoD 1 MSN-0212: import-sites folded into import-scanner (string-aware blankComments); propose/check/verify/runtime share one walker and empty-roots=changed dirty-file policy (no-HEAD unions ls-files --cached, argv ends with --, existsSync drops deleted); gitChildEnv strips GIT_DIR/GIT_WORK_TREE on a copy; draft tokens always emit v3; gantry contract check: OK — no import-site violations; npm test: tests 751 pass 751 fail 0"
    anchor: "947"
    status: PASS
interrogation:
  - finding_id: 0aab8fc3443c
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: Execute MSN-0212 first under gantry (src/cli/, gate npm test).
  - finding_id: 6f9d4d2bf647
    kind: adr_conflict
    question: Intent may relate to ADR-0030. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0030 before
      legislation proceeds.
    operator_answer: "Accept ADR-0030: extend import-scanner.ts (regex/single-pass,
      no TSC API); delete the parallel contract regex engine."
    adr_refs:
      - ADR-0030
  - finding_id: a7bb97b7d55a
    kind: adr_conflict
    question: Intent may relate to ADR-0022. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0022 before
      legislation proceeds.
    operator_answer: "Reject applicability: MSN-0212 does not change MCP
      write-containment; ADR-0022 stays in force for write tools."
    adr_refs:
      - ADR-0022
  - finding_id: c381e0aea4c3
    kind: risk_escalation
    question: "Triage escalated: Intent references path under path_risks: src/cli/.
      Which skill_key and TMVC scope authorize this work?"
    hypothesis: Planner assigns explicit skill_key and narrows TMVC in the mission
      after operator confirmation.
    operator_answer: Execute MSN-0212 first under gantry (src/cli/, gate npm test).
    adr_refs:
      - ADR-0045
  - finding_id: e98547caadb1
    kind: adr_conflict
    question: Intent may relate to ADR-0045. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0045 before
      legislation proceeds.
    operator_answer: "Accept ADR-0045: this mission implements the sealed-cage model
      in src/cli/. Empty-roots=changed is the scan policy; the ADR sentence
      itself waits for MSN-0213."
    adr_refs:
      - ADR-0045
interrogation_sha256: 93b55df83fccca7de80493c408cf8c0559c025ab4cddb3990614684bf52641b4
declared_paths:
  - .gitagent/missions/MSN-0212.fold-contract-import-site-engine-into-canonical-.yaml
  - src/cli/
contract:
  tmvc_roots:
    - src/cli/
  forbidden_zones:
    - .gitagent/foreman/
    - .gitagent/planner/RULES.md
    - src/auth/
    - src/core/
    - src/db/schema/
  allow_dynamic_specifiers: true
contract_sha256: fadb2dc10a3a2533b55525c8fa95551df5e0a1d52e623dc8a3c1f7eb85892a9c
