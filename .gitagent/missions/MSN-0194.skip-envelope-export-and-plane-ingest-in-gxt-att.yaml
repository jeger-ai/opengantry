# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Skip envelope export and plane ingest in gxt-attest-ingest when GANTRY_ORG_ID/GANTRY_ORG_PEPPER or PLANE_INGEST_URL/PLANE_INGEST_TOKEN are unset, so mission PRs do not fail CI for missing optional control-plane config. Still run gantry verify without --export. Keep export+ingest when configured. Mirror the change in templates/.github/workflows/gxt-attest-ingest.yml and document the skip in docs/INTEGRATIONS.md.
msn_id: MSN-0194
skill_key: gantry
gate_command: npm test
trace_rows:
  - dod_id: "1"
    trace_quote: "DoD 1 MSN-0194: gxt-attest-ingest skips --export when GANTRY_ORG_ID or GANTRY_ORG_PEPPER unset and skips ingest when PLANE_INGEST_URL or PLANE_INGEST_TOKEN unset; verify still runs; configured path unchanged; template mirrored; INTEGRATIONS.md documents skip; npm test 637 pass"
    anchor: "831"
    status: PASS
interrogation:
  - finding_id: 2121e0ae69d9
    kind: adr_conflict
    question: Intent may relate to ADR-0037. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0037 before
      legislation proceeds.
    operator_answer: "Accept ADR-0037 ingest contract when plane vars are
      configured: verify --export then POST ingest then poll chained, then
      propagate verify failure. When org/plane vars are unset, skip
      export/ingest and still run verify without --export so missing optional
      hub config is not a PR failure."
    adr_refs:
      - ADR-0037
  - finding_id: 95083290c452
    kind: adr_conflict
    question: Intent may relate to ADR-0034. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0034 before
      legislation proceeds.
    operator_answer: "Accept ADR-0034: hub ingest is optional. When GitHub
      vars/secrets for org export or plane ingest are unset, skip --export and
      ingest instead of failing the PR. Do not change the envelope contract."
    adr_refs:
      - ADR-0034
  - finding_id: 9ecdd9a8e878
    kind: adr_conflict
    question: Intent may relate to ADR-0027. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0027 before
      legislation proceeds.
    operator_answer: Reject applicability — ADR-0027 is SARIF/JUnit verify --format
      exporters. This mission only skips hub envelope --export in CI when
      org/plane vars are unset.
    adr_refs:
      - ADR-0027
  - finding_id: c66111932eee
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: "Skill gantry. TMVC: src/cli/tests for a workflow-contract
      test. Workflow YAML and docs/INTEGRATIONS.md are outside gantry TMVC
      (src/cli/ + package.json); record Context Requests. Do not change Foreman
      routing."
  - finding_id: c75db18757f4
    kind: adr_conflict
    question: Intent may relate to ADR-0036. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0036 before
      legislation proceeds.
    operator_answer: "Accept ADR-0036: org_id and pepper stay out of committed
      substrate. CI skips --export when GANTRY_ORG_* vars/secrets are unset
      rather than writing ORG.export.local."
    adr_refs:
      - ADR-0036
interrogation_sha256: 48ca6b7304f01e75e2aa225b99bfb43f6fc5a980e3e5e89c3c5c8d749f21ab64
declared_paths:
  - .github/workflows/gxt-attest-ingest.yml
  - .gitagent/missions/MSN-0194.skip-envelope-export-and-plane-ingest-in-gxt-att.yaml
  - docs/INTEGRATIONS.md
  - EXECUTOR_LOG.md
  - src/cli/tests/attest-ingest-workflow.test.ts
  - templates/.github/workflows/gxt-attest-ingest.yml
