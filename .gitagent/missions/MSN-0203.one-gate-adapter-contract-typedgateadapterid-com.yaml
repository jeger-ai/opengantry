# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: One gate-adapter contract: TypedGateAdapterId, Commander .choices() for --gate-adapter, GateSpec-shaped legislate gate options, function-typed adapters with one findingsFromRun, Record registry, drop substring-stream-scan.
msn_id: MSN-0203
skill_key: gantry
gate_command: npm test
trace_rows:
  - dod_id: "1"
    trace_quote: REPLACE_WITH_VERBATIM_QUOTE_FROM_EXECUTOR_LOG_AFTER_EXECUTION
    anchor: "1"
    status: PENDING
interrogation:
  - finding_id: 8ff287217765
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: "Confirmed: program-core.ts, program-workflow.ts, and
      commands/doctor.ts are gantry registrar files under src/cli/."
  - finding_id: d585ecf2924b
    kind: risk_escalation
    question: "Triage escalated: No confident skill_key match in intent (ambiguous
      or missing). Which skill_key and TMVC scope authorize this work?"
    hypothesis: Planner assigns explicit skill_key and narrows TMVC in the mission
      after operator confirmation.
    operator_answer: skill_key gantry; TMVC src/cli/ and package.json. Work is
      ADR-0041 adapter routing cleanup only.
    adr_refs:
      - ADR-0041
interrogation_sha256: 91bffea5e845cab2a0a28c41571fb9176e917c88197a36953bc0c7a3dc2701d8
declared_paths:
  - src/cli/commands/doctor.ts
  - src/cli/lib/gate-adapters/registry.ts
  - src/cli/lib/legislate-gate-options.ts
  - src/cli/lib/types.ts
  - src/cli/program-core.ts
  - src/cli/program-workflow.ts
