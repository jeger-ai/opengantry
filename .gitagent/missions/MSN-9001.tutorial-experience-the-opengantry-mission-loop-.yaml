# OpenGantry mission scaffold (Teacher: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Tutorial: experience the OpenGantry mission loop (init --tutorial)
msn_id: MSN-9001
skill_key: logic
gate_command: gapman check
trace_rows:
  - dod_id: "1"
    trace_quote: REPLACE_WITH_VERBATIM_QUOTE_FROM_WORKER_LOG_AFTER_EXECUTION
    anchor: "1"
    status: PENDING

# Teacher stamp: tutorial legislation
