# OpenGantry mission scaffold (Teacher: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Mission C CLI system touches: doctor/status parity, atomic upgrade with Windows-safe promote
msn_id: MSN-0023
skill_key: gapman
gate_command: npm run validate
trace_rows:
  - dod_id: "1"
    trace_quote: REPLACE_WITH_VERBATIM_QUOTE_FROM_WORKER_LOG_AFTER_EXECUTION
    anchor: "1"
    status: PENDING
