# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: src facade cleanup: Phase A facade deletion and Phase B microshard consolidation
msn_id: MSN-0156
skill_key: gantry
gate_command: npm run build && npm test
declared_paths:
  - .gitagent/missions/MSN-0156.src-facade-cleanup-phase-a-facade-deletion-and-p.yaml
trace_rows:
  - dod_id: "1"
    trace_quote: "DoD 1 MSN-0156: Phase A — deleted mcp-governance, program-stdin, doctor barrel; lazy domain registry; VerifyOptions single home; npm test 533 pass"
    anchor: "1"
    status: PASS
  - dod_id: "2"
    trace_quote: "DoD 2 MSN-0156: Phase B — merged verify context/receipt into verify-run, thin MCP into mcp-runtime, folded program registrars; lib-cycles OK (160 files)"
    anchor: "2"
    status: PASS
  - dod_id: "3"
    trace_quote: "DoD 3 MSN-0156: npm run validate OK — build, test, doctor, changed-code, MSN subjects"
    anchor: "3"
    status: PASS
interrogation:
  - finding_id: d585ecf2924b
    kind: risk_escalation
    question: "Triage escalated: No confident skill_key match in intent (ambiguous
      or missing). Which skill_key and TMVC scope authorize this work?"
    hypothesis: Planner assigns explicit skill_key and narrows TMVC in the mission
      after operator confirmation.
    operator_answer: skill_key=gantry; TMVC scope src/cli/ and package.json if knip
      script added. Phase A deletes facades (mcp-governance, program-stdin,
      doctor barrel, upgrade re-exports); lazy domain registry; VerifyOptions
      single home. Phase B merges verify microshards, thin MCP into mcp-runtime,
      folds tiny program registrars into program-core.
    adr_refs:
      - ADR-0033
      - ADR-0022
  - finding_id: e3ab39fda7e3
    kind: missing_test_criteria
    question: Gate command is not on the skill allowlist. Authorize this gate and
      its success criteria on the record.
    hypothesis: Planner accepts gate_command "npm run build && npm test" with
      documented success substring for verify.
    operator_answer: "Authorized: gate_command npm run build && npm test; success
      when build completes and all node tests pass (exit 0)."
interrogation_sha256: 5c54f6a67b094b244673afb296cded145177ed0e7ccdb29e2584d9d96529cbd8
