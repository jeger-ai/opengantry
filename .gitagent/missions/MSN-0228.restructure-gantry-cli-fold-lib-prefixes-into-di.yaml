# OpenGantry mission scaffold (Planner: fill gate, TMVC narrowing, trace rows).
# Legislated intent: Restructure gantry CLI: fold lib prefixes into directories with git mv, split mission pin resolution out of the parser, and make architecture rules a discriminated union. applies_to is a selector on both import and pattern rules, not a kind. The architecture parser stays inside the changed-file line and complexity budgets.
msn_id: MSN-0228
skill_key: gantry
gate_command: npm test
trace_rows:
  - dod_id: "1"
    trace_quote: REPLACE_WITH_VERBATIM_QUOTE_FROM_EXECUTOR_LOG_AFTER_EXECUTION
    anchor: "1"
    status: PENDING
interrogation:
  - finding_id: 0aab8fc3443c
    kind: undefined_boundary
    question: Paths are not mapped to any TARGET_ARCHITECTURE layer (tier Tier-2).
      Confirm boundary for all listed paths.
    hypothesis: Operator authorizes work within declared TMVC and documents boundary
      decision on the record.
    operator_answer: Yes. Authorize the restructure inside src/cli/ (gantry TMVC).
      Do not touch .gitagent/.
  - finding_id: 9b42a6bc8d96
    kind: adr_conflict
    question: Intent may relate to ADR-0026. Accept ADR decision, reject
      applicability, or cite superseding rationale?
    hypothesis: Operator explicitly accepts or rejects ADR ADR-0026 before
      legislation proceeds.
    operator_answer: Accept ADR-0026 — it stays binding; this mission does not change arch fetch
    adr_refs:
      - ADR-0026
interrogation_sha256: f111f130af6ecd972cf4b0a7463b9d74c6fb0f3c12fc547a3be3657383f5f39d
declared_paths:
  - .gitagent/missions/MSN-0228.restructure-gantry-cli-fold-lib-prefixes-into-di.yaml
  - TARGET_ARCHITECTURE.yaml
  - src/cli/
contract:
  tmvc_roots:
    - src/cli/
  forbidden_zones:
    - .gitagent
    - .gitagent/foreman/
    - .gitagent/planner/RULES.md
    - src/auth/
    - src/core/
    - src/db/schema/
  allowed_imports:
    - ${spec}
    - ${v.specifier}
    - (.+)
    - "@app/data/db"
    - "@app/lib"
    - "@app/lib/util"
    - "@clack/prompts"
    - "@modelcontextprotocol/sdk/server/mcp.js"
    - "@modelcontextprotocol/sdk/server/stdio.js"
    - ajv
    - ajv-formats
    - ajv/dist/2020.js
    - also-commented
    - axios
    - banned-pkg
    - better-sqlite3
    - commander
    - commented-out
    - express
    - keep
    - left-pad
    - lodash/esm
    - m
    - pg
    - pkg-a
    - pkg-b
    - pkg-c
    - pkg-ns
    - prisma
    - prisma/client
    - react
    - side
    - sqlite-vec
    - typescript
    - x
    - yaml
    - zod
  banned_imports:
    - "@modelcontextprotocol/sdk/client/index.js"
    - "@modelcontextprotocol/sdk/client/stdio.js"
  allow_dynamic_specifiers: true
contract_sha256: 0bad465fb6e19444f5dcbba56f49b9ce916c03669216396ed1a01a1d1a6171bf
