# Skill: gapman-ralph

Manifest key `gapman-ralph`. **gapman** CLI and tests under `src/cli/` per [`.gitagent/foreman/MANIFEST.json`](../.gitagent/foreman/MANIFEST.json).

Substrate paths (`.gitagent/`, hooks, workflows) require Teacher legislation and **`substrate-ralph`** or explicit mission scope — do not edit Foreman law without a mission.
