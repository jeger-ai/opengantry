## Problem

Three Commander `.action()` handlers in `program-core.ts` (triage, start) and `program-workflow.ts` (legislate) repeat the same stdin-or-argv intent resolution and empty-input error handling (~8 lines each). `readStdinIfEmpty()` is defined in `commands/triage.ts` but consumed by program registrars, inverting the usual dependency direction. Related option-bag copy-through in init/verify registrars is tracked separately as **#55**.

## Acceptance

- [ ] Extract shared helper (e.g. `lib/cli-intent.ts` or extend `cli-io.ts`) returning `{ intent: string } | { error: true }`
- [ ] Replace duplicated blocks in `program-core.ts` and `program-workflow.ts`
- [ ] Move `readStdinIfEmpty` out of `commands/triage.ts` into lib (triage command imports lib)
- [ ] Behavior unchanged: empty argv + empty stdin → exit 2 with same error messages
- [ ] No change required for `arch cred set` stdin flow

## Priority

**Low** — ergonomics only; post-v1.1.1 tactical; overlaps conceptually with **#55** but scoped to stdin.

**Refs:** docs/BACKLOG.md (tactical); related **#55** (option-bag copy-through)
