## Problem

`legislative_commits` / `worker_trace_commits` are path-touch proxies, not triage replay. `git-metrics-stream.ts` uses `parseTeacherEmailsFromEnv()` only for legislative classification — not `resolveTeacherEmails(root)` used by git-proof. `worker_trace` counts any WORKER_LOG-only commit, not trace-attested PASS rows. `docs/ADOPTION.md` already caveats this but dashboards may misreport activity.

## Acceptance

- [ ] `aggregateFromLogStream` uses `resolveTeacherEmails(repoRoot)` (same precedence as git-proof)
- [ ] Document or refine `worker_trace` definition (e.g. require `[MSN-…]` in subject, or WORKER_LOG diff with PASS quotes)
- [ ] Optional `--explain` / JSON `commit_classifications[]` for audit
- [ ] Tests with repo `TEACHER.allowlist` vs env-only scenarios
- [ ] Update `docs/ADOPTION.md` metrics section with new semantics

## Priority

**Medium** — observability; not blocking verify.

**Refs:** docs/BACKLOG.md, docs/ADOPTION.md
