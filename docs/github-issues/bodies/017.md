## Problem

Break-glass bypass for verify is implemented (v0.8.0+: `break-glass.ts`, git notes `refs/notes/gxt-bypass`, doctor checks, metrics) but the **end-to-end auditable protocol** is incomplete. Orchestration is duplicated across command/flow layers (#44 remainder); post-incident Teacher review is manual; bypass usage is metrics-only; adopters lack a single compliance runbook tying secret provisioning, note push, CI acceptance, and forbidden-zone non-bypass into one enforced lifecycle. Break-glass **does not** skip forbidden-zone enforcement in `runtime exec` (by design — RULES §6.2).

## Acceptance

- [ ] Re-scope as **"complete v1.2 break-glass protocol"** (builds on shipped v0.8.0 core)
- [ ] Single verify orchestration path for break-glass (#44 remainder merged)
- [ ] Doctor warns when `refs/notes/gxt-bypass` notes exist locally but are unpushed, or when bypass used on branch without matching CI fetch step documented
- [ ] `gapman metrics` / optional `gapman audit bypass` surfaces last N bypass events with mission/msn/reason for Teacher review
- [ ] RULES + `docs/ADOPTION.md` + `docs/COMPLIANCE-ISO.md` document mandatory post-incident Teacher mission template
- [ ] Tests: CI parity for note validation; break-glass cannot disable `runtime exec` FZ scan (regression)

## Priority

**Major** — security/compliance adjacent; smaller delta than #14 because core exists; pair with #44 cleanup. Requires ADR + Teacher mission.

**Refs:** docs/BACKLOG.md (v1.2+); RULES.md §6.2
