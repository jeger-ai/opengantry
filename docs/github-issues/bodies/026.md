## Problem

TMVC expansion is free-text in `WORKER_LOG.md`. Workers lack a structured way to propose paths; Teachers/Verifiers lack machine-checkable ACCEPTED/REJECTED records; verify cannot distinguish approved expansion vs scope violation. RULES § Dynamic TMVC requires requests recorded in `WORKER_LOG.md`; no CLI scaffolds the format or accept/reject flow. Only code mention is a remediation hint in `agent-error.ts`.

## Acceptance

- [ ] `gapman context request --mission … --paths … --reason …` appends a canonical block to `WORKER_LOG.md`
- [ ] Optional `gapman context accept|reject` for Teacher/Verifier (or document manual ACCEPTED line format + validator)
- [ ] Verify or new scope check respects ACCEPTED paths for stale/TMVC checks
- [ ] MCP tool parity (`gxt_context_request`) for IDE agents
- [ ] Template snippet in `templates/.gitagent/teacher/WORKER_LOG.template.md`
- [ ] Tests for parse + verify interaction

## Priority

**Medium** — policy exists; tooling gap; pairs with #25.

**Refs:** docs/BACKLOG.md, RULES.md § Dynamic TMVC
