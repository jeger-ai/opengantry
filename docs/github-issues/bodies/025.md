## Problem

Workers can stage/commit files outside mission TMVC before verify runs. Enforcement is late (verify stale trace in `trace-evidence.ts`) or advisory (IDE rules). `.githooks/` has `pre-push` and `post-checkout` only — **no pre-commit**. `runtime exec` enforces forbidden zones, not "stay inside TMVC roots." Policy in RULES §4 (Dynamic TMVC) has no git hook implementation.

## Acceptance

- [ ] New `templates/.githooks/pre-commit` + template sync via `init-asset-catalog.ts`
- [ ] Hook calls `gapman` (new subcommand or shared lib) — **no duplicated MSN/TMVC logic in shell** per `docs/ARCHITECTURE.md`
- [ ] Respects pinned mission via `scripts/gxt-resolve-mission.sh`
- [ ] Clear error listing offending paths + `gapman runtime env` hint
- [ ] Document opt-out / advisory vs enforced tiers; env flag OK for bypass
- [ ] Tests for path classification reusing manifest helpers (similar to `scripts/gxt-manifest-lib.mjs`)

## Priority

**Major** — security/containment; design needed for Context Request interaction (#26).

**Refs:** docs/BACKLOG.md (tactical), RULES.md §4
