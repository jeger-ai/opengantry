## Problem

`init-tutorial.ts` imports `runVerify` from `commands/verify.js` (line 17), violating the lib→command boundary tracked in `check-import-layers.test.ts`. Step 5 reads `process.exitCode` after `runVerify` (lines 75–78, 125–132) instead of using typed `evaluateVerifyPhases()` from `verify-engine.ts`. This is one of three lib→command violations; the root fix is extracting `lib/verify-run` (and `lib/legislate-core`) under **#43**, not a standalone tutorial patch.

## Acceptance

- [ ] **Do not implement in isolation** — track under **#43**
- [ ] `init-tutorial.ts` imports verify logic from `lib/verify-run` (or equivalent), not `commands/verify.js`
- [ ] Replace `verifyExitOk()` / `process.exitCode` with typed phase result
- [ ] `check-import-layers.test.ts` no longer lists `init-tutorial.ts` as violation
- [ ] Close **#11** when **#43** merges; `gapman init --tutorial` still passes MSN-9001 loop

## Priority

**Blocker** (within v1.1.1) — architectural violation; effectively part of **#43**, not standalone work.

**Superseded by broader scope:** [#43](https://github.com/jeger-ai/opengantry/issues/43) covers init-tutorial plus `mcp-legislation.ts` and `start-orchestration.ts`. Close this issue when #43 ships.

**Refs:** docs/BACKLOG.md v1.1.1; thermo-nuclear review (F1)
