## Problem

WORKER_LOG is the forensic source for verify trace mapping, but **`gapman doctor` never validates log hygiene**. Teams discover formatter drift, missing `.prettierignore` entries, merge conflict markers, or duplicate DoD lines only at verify failure — undermining v1.1 formatter guard (MSN-0027) and stale-evidence binding. Doctor checks manifest, Teacher allowlist, hooks, architecture pointer, substrate drift — but not WORKER_LOG integrity.

## Acceptance

- [ ] Doctor lines (warn unless `--strict-worker-log`): WORKER_LOG exists; path matches default or pinned mission's `GXT_WORKER_LOG`
- [ ] `.prettierignore` (or biome/eslint equivalent) contains exact `WORKER_LOG.md` line — reuse merge helper from `file-merge-gxt.ts`
- [ ] Detect merge conflict markers, duplicate DoD lines, or formatter churn patterns
- [ ] Optional: detect CRLF-only or mixed line endings; suggest `.gitattributes`
- [ ] When pinned mission exists: parse trace_rows and warn if quotes are still legislative placeholders
- [ ] `gapman doctor --json` includes structured `worker_log` section; actionable fix hints
- [ ] Tests in `doctor.test.ts`; docs cross-link ADOPTION formatter guard

## Priority

**Minor** — quick win, builds on v1.1 shipped work; good first v1.2 mission after v1.1.1. Requires ADR + Teacher mission.

**Refs:** docs/BACKLOG.md (v1.2+)
