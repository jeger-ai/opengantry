## Problem

The specimen `MANIFEST.json` shipped by `gapman init` declares `ui` → `src/components/`, `src/styles/` and `logic` → `src/lib/`, `src/utils/`, but the OpenGantry repo has no such directories — dogfood routes through `gapman` → `src/cli/`. This confuses contributors and agents despite warnings in `docs/DEVELOPMENT.md` and `PROJECT_OUTLINE_ANALYSIS.md`. `gxt-manifest-lib.mjs` unions all skill `tmvc_roots` for MSN-enforced prefixes, so phantom paths pollute triage and CI checks.

## Acceptance

- [ ] Decide vs **#31**: template fix (#13) or dogfood-only overlay (#31)
- [ ] If #13: update `templates/.gitagent/foreman/MANIFEST.json` — ui/logic use document/skill-based routing or empty `tmvc_roots` with clear `desc`
- [ ] Sync `path_risks` if fictional Tier-3 paths are also removed or commented
- [ ] Update `init-tutorial.ts` skill pick order and adoption docs
- [ ] `gapman check`, `validate-gxt.test.ts`, MSN-9001 tutorial mission still pass
- [ ] Adopter migration note in `docs/ADOPTION.md` for repos that relied on default specimen paths

## Priority

**Minor** — narrative / routing clarity for adopters and dogfood; choose **#13 vs #31** before implementing.

**Refs:** docs/BACKLOG.md (tactical), PROJECT_OUTLINE_ANALYSIS.md; alternative **#31**
