## Problem

`verify.test.ts` is a 548-line monolith with 17 cases mixing break-glass, gate/trace, pre-push, fix-hints, and git-proof scenarios. Each test repeats mkdtemp → fixture → chdir → teardown (~15–20 lines each). `verify-json.test.ts` already covers JSON separately, so the monolith mainly blocks focused edits to CLI verify behavior. BACKLOG notes ~45 integration tests repeat the same boilerplate; `withMiniRepo` / `miniVerifiedMission()` do not exist yet.

## Acceptance

- [ ] Split into phase-focused files (e.g. `verify-break-glass.test.ts`, `verify-gate-trace.test.ts`, `verify-fix-hints.test.ts`; skip new JSON file — `verify-json.test.ts` exists)
- [ ] Add `withMiniRepo()` (and optional named scenarios like `miniVerifiedMission()`) to `test-fixtures.ts` or `test-shared.ts`
- [ ] Each resulting file stays under the 900-line test budget
- [ ] No test case dropped; full verify test suite passes
- [ ] Land after **#44** if verify internals are still in flux

## Priority

**Minor** — v1.1.1 backlog item; improves test ergonomics only.

**Refs:** docs/BACKLOG.md v1.1.1; thermo-nuclear review #2
